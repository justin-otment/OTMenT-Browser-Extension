name: Playwright Extension E2E
on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  e2e:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm'

      - name: Install deps
        run: npm ci

      - name: Install xvfb
        run: sudo apt-get update && sudo apt-get install -y xvfb

      - name: Run Playwright tests under Xvfb
        env:
          DISPLAY: :99
        run: |
          nohup Xvfb :99 -screen 0 1920x1080x24 &> /tmp/xvfb.log &
          node ./tests/run-tests.js
        timeout-minutes: 20

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-artifacts
          path: |
            artifact-screenshot.png
            /tmp/xvfb.log
