name: Browser tests (non-headless)

on: [push, pull_request]

permissions:
  contents: read
  actions: write

jobs:
  browser:
    runs-on: ubuntu-latest

    steps:
      # ===========================
      # 1. Setup and Preparation
      # ===========================
      - name: 🧩 Checkout repository
        uses: actions/checkout@v4

      - name: 🧱 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 🧰 Install system dependencies
        run: |
          sudo apt-get update -y
          sudo apt-get install -y software-properties-common
          sudo add-apt-repository -y universe
          sudo apt-get update -y
          sudo apt-get install -y --no-install-recommends \
            xvfb libgtk-3-0 libxss1 libasound2t64 fonts-liberation \
            libgbm-dev libnss3-dev libu2f-udev xdg-utils openvpn \
            iproute2 iputils-ping curl python3 python3-pip jq

      - name: 🌐 Setup Chrome (Chromium)
        uses: browser-actions/setup-chrome@v1
        with:
          chrome-version: latest
          install-dependencies: true
          install-chromedriver: false

      - name: 💾 Cache npm dependencies
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: npm-cache-${{ runner.os }}-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            npm-cache-${{ runner.os }}-

      - name: 💾 Cache Puppeteer Chromium
        uses: actions/cache@v4
        with:
          path: ~/.cache/puppeteer
          key: puppeteer-${{ runner.os }}-${{ hashFiles('package-lock.json') }}
          restore-keys: puppeteer-${{ runner.os }}-

      - name: 📦 Install npm packages (robust)
        env:
          CI: true
        run: |
          set -euo pipefail
          echo "Attempting npm ci (deterministic)..."
          if npm ci --ignore-scripts --no-audit --no-fund; then
            echo "✅ npm ci succeeded"
          else
            echo "⚠ npm ci failed — falling back to npm install --legacy-peer-deps"
            npm install --legacy-peer-deps --no-audit --no-fund
          fi

      - name: 🧩 Export Chrome binary for Puppeteer
        run: |
          CHROME_BIN=$(which google-chrome || which chromium || true)
          echo "✅ Found Chrome binary: $CHROME_BIN"
          echo "PUPPETEER_EXECUTABLE_PATH=$CHROME_BIN" >> $GITHUB_ENV
          echo "CHROME_PATH=$CHROME_BIN" >> $GITHUB_ENV

      - name: 🧠 Verify Puppeteer Chromium setup
        run: |
          set -e
          echo "🔍 Checking Puppeteer setup..."
          node - <<'EOF'
          import puppeteer from 'puppeteer-core';
          const browserPath = process.env.PUPPETEER_EXECUTABLE_PATH;
          (async () => {
            try {
              const browser = await puppeteer.launch({
                headless: true,
                executablePath: browserPath,
                args: ['--no-sandbox', '--disable-setuid-sandbox', '--incognito']
              });
              const version = await browser.version();
              console.log("✅ Puppeteer successfully launched:", version);
              await browser.close();
            } catch (err) {
              console.error("❌ Puppeteer sanity check failed:");
              console.error(err);
              process.exit(1);
            }
          })();
          EOF

      - name: 🕵️ Locate Chrome binary
        id: chrome-bin
        run: |
          CHROME_BIN=$(which google-chrome || which chromium || true)
          if [ -z "$CHROME_BIN" ]; then
            CHROME_BIN=$(find /opt/hostedtoolcache/setup-chrome -type f -name chrome | head -n1)
          fi
          if [ -z "$CHROME_BIN" ]; then
            echo "❌ Chrome binary not found."; exit 1
          fi
          echo "✅ Using Chrome binary: $CHROME_BIN"
          echo "CHROME_BIN=$CHROME_BIN" >> $GITHUB_ENV
          "$CHROME_BIN" --version

      - name: 🗂️ Prepare artifact directories
        run: mkdir -p artifacts/{screenshots,diagnostics,videos}

      # ===========================
      # 2. VPN Connection
      # ===========================
      - name: 🔁 Rotate and connect OpenVPN
        run: |
          # (unchanged VPN setup script)
          echo "🚀 Starting OpenVPN..."
          # ...

      # ===========================
      # 3. Chrome + Extension Setup
      # ===========================
      - name: ⚙️ Detect and inject unpacked Chrome extension
        run: |
          set -euo pipefail
          TMP_EXT="/tmp/otment-extension"
          rm -rf "$TMP_EXT"
          mkdir -p "$TMP_EXT"

          EXT_SRC=$(find "${GITHUB_WORKSPACE}" -maxdepth 3 -name "manifest.json" -printf '%h\n' | head -n1)
          if [ -z "$EXT_SRC" ]; then
            echo "❌ manifest.json not found in repository — cannot load extension"
            exit 1
          fi

          echo "📦 Copying Chrome extension from: $EXT_SRC"
          cp -r "$EXT_SRC/." "$TMP_EXT/"
          sudo chmod -R 755 "$TMP_EXT" || true

          echo "EXTENSION_PATH=$TMP_EXT" >> $GITHUB_ENV
          # 👇 Added --incognito flag here
          echo "CHROME_ARGS=--no-sandbox --disable-dev-shm-usage --disable-gpu --window-size=1920,1080 --incognito --disable-extensions-except=$TMP_EXT --load-extension=$TMP_EXT" >> $GITHUB_ENV
          echo "✅ Extension ready at: $TMP_EXT"

      # ===========================
      # 4. Orchestrator Runner (CI)
      # ===========================
      - name: 🚀 Run OTMenT CI Orchestrator
        run: bash scripts/orchestrator-ci.sh

      # ===========================
      # 5. Post-Run Cleanup
      # ===========================
      - name: 🧾 Collect diagnostics and screenshots
        if: always()
        run: |
          ls -la artifacts/screenshots || true
          [ -f /tmp/openvpn.log ] && cp /tmp/openvpn.log artifacts/diagnostics/openvpn.log || true

      - name: 🔌 Disconnect VPN
        if: always()
        run: |
          sudo pkill -f openvpn || true
          echo "✅ VPN disconnected."

      - name: 📤 Upload test artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: browser-artifacts
          path: |
            artifacts/diagnostics/**
            artifacts/screenshots/**
            artifacts/videos/**
