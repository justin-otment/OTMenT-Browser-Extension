name: Browser tests (non-headless)

on: [push, pull_request]

permissions:
  contents: read
  actions: write

jobs:
  browser:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install system dependencies
        run: |
          sudo apt-get update -y
          sudo apt-get install -y software-properties-common
          sudo add-apt-repository -y universe
          sudo apt-get update -y
          sudo apt-get install -y --no-install-recommends \
            xvfb libgtk-3-0 libxss1 libasound2t64 fonts-liberation \
            libgbm-dev libnss3-dev libu2f-udev xdg-utils openvpn \
            iproute2 iputils-ping curl

      - name: Setup Chrome (Chromium)
        uses: browser-actions/setup-chrome@v1
        with:
          chrome-version: latest
          install-dependencies: true
          install-chromedriver: false

      - name: Install npm packages (tolerant)
        run: |
          set -euo pipefail
          echo "Attempting npm ci..."
          if ! npm ci; then
            echo "npm ci failed — lockfile likely outdated."
            npm install --legacy-peer-deps
            npm ci || npm install
          fi

      - name: Locate Chrome binary
        id: chrome-bin
        run: |
          CHROME_BIN=$(which google-chrome || which chromium || true)
          if [ -z "$CHROME_BIN" ]; then
            CHROME_BIN=$(find /opt/hostedtoolcache/setup-chrome -type f -name chrome | head -n1)
          fi
          if [ -z "$CHROME_BIN" ]; then
            echo "❌ Chrome binary not found."; exit 1
          fi
          echo "✅ Using Chrome binary: $CHROME_BIN"
          echo "CHROME_BIN=$CHROME_BIN" >> $GITHUB_ENV
          "$CHROME_BIN" --version

      - name: Ensure chromedriver (diagnostic)
        run: |
          ls -la node_modules/.bin || true
          node -e "console.log(require('chromedriver')?.path || 'chromedriver not found')" || true

      - name: Prepare artifacts directories
        run: mkdir -p artifacts/{screenshots,diagnostics,videos}

      - name: Verify or create test page
        run: |
          if [ ! -f "otment-test.html" ]; then
            echo "⚠️ otment-test.html missing, creating fallback..."
            cat <<'EOF' > otment-test.html
          <!DOCTYPE html>
          <html lang="en">
          <head><meta charset="UTF-8"><title>OTMenT Test Page</title></head>
          <body>
            <div id="otment-status">Initializing...</div>
            <script>
              setTimeout(()=>{document.getElementById('otment-status').classList.add('active');},1500);
            </script>
          </body>
          </html>
          EOF
          fi
          echo "✅ otment-test.html ready."

      - name: Start OpenVPN (rotate configs)
        env:
          CI: true
        run: |
          set -euo pipefail
          VPN_DIR="VPN"
          AUTH_FILE="$VPN_DIR/auth.txt"
          STATE_FILE="$VPN_DIR/rotation_state.txt"
          PIDFILE="/tmp/openvpn.pid"
          LOGFILE="/tmp/openvpn.log"

          mapfile -t OVPN_LIST < <(find "$VPN_DIR" -maxdepth 1 -type f -name "*.ovpn" | sort)
          if [ ${#OVPN_LIST[@]} -eq 0 ]; then
            echo "❌ No .ovpn found in $VPN_DIR"; exit 1
          fi
          [ -f "$AUTH_FILE" ] || { echo "❌ Missing $AUTH_FILE"; exit 1; }

          if [ -f "$STATE_FILE" ]; then
            LAST=$(cat "$STATE_FILE" || true)
            IDX=$(printf '%s\n' "${OVPN_LIST[@]}" | grep -nFx "$LAST" | cut -d: -f1 || echo 0)
            NEXT_INDEX=$(( (IDX) % ${#OVPN_LIST[@]} ))
            NEXT="${OVPN_LIST[$NEXT_INDEX]}"
          else
            NEXT="${OVPN_LIST[0]}"
          fi

          echo "$NEXT" > "$STATE_FILE"
          echo "🧭 Using VPN config: $NEXT"

          BASE_IP=$(curl -sS https://ifconfig.co || echo "unknown")
          echo "🌐 Base IP: $BASE_IP"

          echo "🚀 Starting OpenVPN..."
          if grep -q "auth-user-pass" "$NEXT"; then
            sudo nohup openvpn --config "$NEXT" --auth-user-pass "$AUTH_FILE" --daemon --writepid "$PIDFILE" --log "$LOGFILE"
          else
            sudo nohup openvpn --config "$NEXT" --daemon --writepid "$PIDFILE" --log "$LOGFILE"
          fi

          sleep 3
          [ -f "$PIDFILE" ] || { echo "❌ OpenVPN failed to start"; tail -n 50 "$LOGFILE"; exit 1; }

          echo "⏳ Waiting for tun0..."
          for i in $(seq 1 60); do
            ip a show tun0 &>/dev/null && break
            sleep 1
          done
          ip a show tun0 &>/dev/null || { echo "❌ VPN interface not found"; tail -n 100 "$LOGFILE"; exit 1; }

          VPN_IP=$(curl -sS https://ifconfig.co || echo "unknown")
          echo "VPN IP: $VPN_IP"
          [ "$VPN_IP" != "$BASE_IP" ] && [ "$VPN_IP" != "unknown" ] || {
            echo "❌ VPN connection failed"; tail -n 100 "$LOGFILE"; exit 1;
          }

      - name: Start local test server
        run: |
          npm install -g http-server@14
          nohup npx http-server -p 8080 -c-1 . > /tmp/http-server.log 2>&1 &
          for i in $(seq 1 10); do
            curl -sSf http://127.0.0.1:8080/otment-test.html >/dev/null 2>&1 && break
            sleep 1
          done
          curl -sSf http://127.0.0.1:8080/otment-test.html >/dev/null || {
            echo "❌ Test page not reachable"; cat /tmp/http-server.log; exit 1;
          }

      - name: Export Chrome args
        run: |
          echo "TEST_PAGE_URL=http://127.0.0.1:8080/otment-test.html" >> $GITHUB_ENV
          echo "CHROME_ARGS=--disable-extensions-except=${PWD} --load-extension=${PWD} --enable-unsafe-swiftshader --no-sandbox --disable-dev-shm-usage --window-size=1920,1080" >> $GITHUB_ENV

      - name: Run headed tests under Xvfb (with Chrome path export)
        env:
          CI: true
          TEST_PAGE_URL: ${{ env.TEST_PAGE_URL }}
          CHROME_ARGS: ${{ env.CHROME_ARGS }}
          PUPPETEER_SKIP_CHROMIUM_DOWNLOAD: "1"
        run: |
          set -euo pipefail
          # ensure Chrome path exported for puppeteer-core
          CHROME_PATH="${{ env.CHROME_BIN }}"
          if [ -z "$CHROME_PATH" ]; then
            CHROME_PATH=$(which google-chrome || which chromium || find /opt/hostedtoolcache/setup-chrome -type f -name chrome | head -n1)
          fi
          echo "Using CHROME_PATH=$CHROME_PATH"
          export CHROME_PATH

          "$CHROME_PATH" --version || true
          node -e "console.log('Puppeteer will use:', process.env.CHROME_PATH)"

          xvfb-run --auto-servernum --server-args="-screen 0 1920x1080x24" npm run test:headed || RC=$?

          mkdir -p artifacts/diagnostics
          if [ -f /tmp/openvpn.log ]; then
            # Copy log safely and fix ownership for upload
            sudo cp /tmp/openvpn.log artifacts/diagnostics/openvpn.log || true
            sudo chmod 644 artifacts/diagnostics/openvpn.log || true
            sudo chown $(whoami):$(whoami) artifacts/diagnostics/openvpn.log || true
          fi

          exit ${RC:-0}
        timeout-minutes: 30

      - name: Verify artifacts
        if: always()
        run: |
          ls -la artifacts || true
          if [ ! -s artifacts/extension-browser-logs.json ]; then
            echo "⚠️ extension-browser-logs.json missing; continuing with diagnostics only"
          fi

      - name: Collect diagnostics
        if: always()
        run: |
          (google-chrome --version || chromium --version || true)
          echo "Artifacts:"
          find artifacts -maxdepth 2 -type f | sed 's/^/  /'
          tail -n 100 /tmp/openvpn.log 2>/dev/null || true

      - name: Disconnect OpenVPN
        if: always()
        run: |
          sudo pkill -f openvpn || true
          sudo rm -f /tmp/openvpn.pid || true

      - name: Upload artifacts
        if: always()
        run: |
          echo "Fixing permissions before upload..."
          sudo chmod -R 755 artifacts || true
          sudo chown -R $(whoami):$(whoami) artifacts || true
          uses: actions/upload-artifact@v4
          with:
            name: browser-artifacts
            path: artifacts/**

